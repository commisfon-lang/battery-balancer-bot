version: '3.8'

services:
  telegram-bot:
    build: .
    container_name: battery-balancer-bot
    restart: unless-stopped
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
    volumes:
      - ./logs:/app/logs